---
layout: "ExtensionPageLayout"
description: "The WYSIWYG mode extension allows users to edit blocks in it's readable, rendered state!"
development: true
---

import {DemoWYSIWYGMode} from "../../../src/components/WYSIWYGMode";

While in a block, you could hit **ALT+w** to turn the block into WYSIWYG (what you see is what you get) mode. 

### Playground

The text editor below is what gets rendered over a block while in WYSIWYG Mode.

<DemoWYSIWYGMode />
