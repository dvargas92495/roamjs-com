---
layout: "ExtensionPageLayout"
description: "The Query Builder extension introduces a new user interface for building queries and outputting the query syntax into the block."
development: true
---

import { DemoWikiData } from "../../../src/components/WikiData";

The script is not configurable.

In a block, type `{{wiki}}`. Similar to date picker, there will be an overlay that appears next to the wiki button.

### Playground

<DemoWikiData />
